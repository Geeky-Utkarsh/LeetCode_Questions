class Solution {
public:
    int strStr(string haystack, string needle) {
    //    string n=find(needle);
    // }
    // if((n.size())>0)
    //    return -1;
    // else 
    //  return n;

    // int fA=0;

    // if(haystack.find(needle)!=string::npos){
    //     fA=haystack.find(needle);
    //     return 0;
    //  }
    // return -1;
    return haystack.find(needle);
 
    }
};